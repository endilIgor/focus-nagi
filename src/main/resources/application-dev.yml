# Development conveniences. Production runs with the default profile plus env vars.
app:
  owner:
    password: ${APP_OWNER_PASSWORD:dev-password-only}
  swagger:
    enabled: true

logging:
  level:
    com.focusnagi: DEBUG
