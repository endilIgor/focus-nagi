spring:
  datasource:
    url: jdbc:tc:postgresql:16-alpine:///focusnagi
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

app:
  owner:
    username: owner
    password: test-password

logging:
  level:
    org.springframework.security: INFO
