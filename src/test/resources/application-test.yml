spring:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

app:
  owner:
    username: owner
    password: test-password
  swagger:
    enabled: true

logging:
  level:
    org.springframework.security: INFO
